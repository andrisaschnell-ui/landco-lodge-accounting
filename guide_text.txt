                  Installing the Accounting App for a New Company                  A step-by-step guide written for non-technical users              This guide walks you through installing a brand-new, fully isolated copy of the accounting application on a single computer, for one company. Every company you install gets its own folder, its own database, and its own login â€” they cannot see each otherâ€™s data.              You only need to read and follow this document once per company. The whole process usually takes 15â€“25 minutes the first time, and most of that is the computer doing the work.                  1. What youâ€™ll end up with              When the installer finishes you will have:                  A web-based accounting app running on this computer that you open in your browser at an address like http://localhost:8080.                  A private database holding only this companyâ€™s data, stored inside Docker.                  Two administrator users (the two people you choose during the install) who can sign in immediately.                  Your company name, NUIT (tax number), address and telephone already saved â€” they appear automatically on invoices, reports and the sidebar.                  Optionally, a starter chart of accounts and a sample property + two shareholders so the app is not completely empty on day one. You can delete any of this later.                  2. Before you start â€” checklist              Please make sure all of the following are true on the computer where you will install the app. If any item is missing, stop and arrange it first.                                               What you need                               Why                              A Windows 10/11 or Mac/Linux computer                            The app runs entirely on this machine.                              Docker Desktop installed and running                            Hosts the database and the web app. Free to download.                              Git for Windows (Windows only)                            Used to download the latest application code.                              Internet connection                            Needed once, during the install, to download files.                              GitHub repository URL                            Provided to you by your IT contact (the &quot;v5&quot; branch).                              Company details                            Legal name, NUIT (tax number), street address, telephone.                              Two admin accounts                            Two emails + two passwords for the people who will manage the system.                        Tip:           Docker Desktop must be visibly running (its whale icon is in the system tray on Windows or the menu bar on Mac). The installer will refuse to continue if Docker is not running.                  3. Get the installer onto your computer              Your IT contact will send you one of the following:                  A direct link to the file install-new-company.bat (Windows) or install-new-company.sh (Mac/Linux), or                  A small ZIP file containing the installer.              Save the file anywhere convenient â€” your Desktop is fine. You do NOT need to put it in the folder where the app will live; the installer will create that folder for you.                  4. Run the installer                  Windows                   Open the folder where you saved install-new-company.bat.                   Double-click install-new-company.bat. A black command-prompt window will open.                   If Windows shows a SmartScreen warning (&quot;Windows protected your PC&quot;), click More info â†’ Run anyway. This warning appears for every script that wasnâ€™t downloaded from the Microsoft Store; it does not mean anything is wrong.                   Read the welcome message and press any key to continue.                  Mac / Linux                   Open Terminal.                   Make the script executable (only the first time):               chmod +x ~/Downloads/install-new-company.sh                   Run it:               ~/Downloads/install-new-company.sh                  5. Answering the installerâ€™s questions              The installer will ask you a series of questions, in plain English, in this order. The table below tells you exactly what to type for each one. Take your time â€” you can stop and look something up before answering. If you make a mistake, press Ctrl+C in the window to cancel, then start over.                                               Prompt                               What to type                              Install folder                            A new folder, e.g. C:\Apps\AcmeBooks (Windows) or ~/apps/acmebooks (Mac/Linux). Create one new folder per company.                              App short name                            A short, lowercase name with no spaces â€” e.g. acme. Used internally for the database and container names.                              Web port                            Press Enter to accept 8080. If you already have another company on this PC using 8080, type 8081 (or 8082, etc.).                              API port                            Press Enter for 4000 (or 4001/4002 for additional companies).                              DB port                            Press Enter for 5432 (or 5433/5434 for additional companies).                              Company legal name                            Exactly as it should appear on invoices and reports â€” e.g. Acme Lda.                              NUIT                            Your 9-digit Mozambican tax number.                              Street address                            Full address including city and &quot;Mozambique&quot;.                              Telephone                            Including country code, e.g. +258 84 123 4567.                              Admin #1 / #2 email                            The email each admin will use to sign in. Real or fake addresses both work â€” they are not sent any email.                              Admin #1 / #2 password                            Minimum 8 characters. Use something strong â€” write it down somewhere safe.                              Admin #1 / #2 display name                            The full name shown next to actions in the audit log.                              Load demo data?                            Type Y for a starter chart of accounts and one sample property + two shareholders. You can delete or edit any of it later. Type n if you want a fully empty database.                              GitHub repo URL                            Provided by your IT contact. Looks like https://github.com/your-org/landco.git.                        Important:           The two admin passwords are stored encrypted in your local database and are never shown again. Write them down somewhere safe (a password manager is ideal). If you lose them, your IT contact can reset them but it requires the computer.                  6. What happens while the installer runs              After you confirm your answers, the installer goes through eight steps. The window shows progress in real time. You donâ€™t need to do anything â€” just leave the window open until it finishes.                  [1/8] Checking that Git, Docker and Docker Desktop are all working.                  [2/8] Asking for installation details (folder, ports).                  [3/8] Asking for company identity.                  [4/8] Asking for the two admin users.                  [5/8] Downloading the latest application code from GitHub (branch v5).                  [6/8] Generating secure configuration files with random passwords for the database.                  [7/8] Building and starting the three Docker containers (database, API, web). This is the slowest step â€” typically 5 to 10 minutes the first time.                  [8/8] Creating the two admin users, saving your company name/NUIT/address/telephone, and (if you said yes) loading the demo data.              When everything finishes, the window turns green and shows you the address to open in your browser plus a reminder of the two sign-in emails.                  7. Signing in for the first time                   Open Google Chrome, Microsoft Edge or any modern browser.                   Go to the address shown at the end of the install â€” usually http://localhost:8080 (or 8081, 8082, etc., if you chose a different port).                   Type the email and password of one of the two admins you created.                   Click Sign in.              You should land on the dashboard. The company name in the top-left sidebar should already be the name you typed during the install.                  8. First things to do after you sign in                  Go to Settings â†’ Identity and upload your company logo. It will replace the placeholder everywhere.                  Go to Settings â†’ Operational and choose a folder where the daily database backup will be saved. A USB drive or a OneDrive/Google Drive folder works well.                  If you loaded demo data, look at the Chart of Accounts and edit/delete any account that doesnâ€™t fit your business.                  Add the rest of your real users from Settings â†’ Users. By default they will be &quot;viewer&quot; (read-only). Promote anyone who needs to enter data to &quot;admin&quot; from the same screen.                  Try entering one real expense to confirm that everything works end-to-end.                  9. Day-to-day usage â€” start, stop, restart              Once installed, the app stays running in the background as long as Docker Desktop is running. You donâ€™t need to do anything special each morning.                  To stop the app (e.g. before turning off the computer)               cd &lt;your install folder&gt;
docker compose stop                  To start it again               cd &lt;your install folder&gt;
docker compose --env-file .env.local up -d                  To check itâ€™s running               docker ps              You should see three containers whose names start with the short app name you chose, ending in -web, -api and -db.                  10. Installing a SECOND company on the same computer              You can run as many companies as you like on one machine. Just run the installer again, and at the prompts:                  Choose a different install folder.                  Choose a different short app name (e.g. ebony instead of acme).                  Choose different ports â€” 8081 / 4001 / 5433 for the second one, 8082 / 4002 / 5434 for the third, and so on.              Each company will appear at its own URL (http://localhost:8081, etc.) and its data is completely separate.                  11. Backups              A daily backup of the database is created automatically and saved to the folder you chose under Settings â†’ Operational. Backups are kept for 30 days; older ones are deleted to save space.              To take a manual backup at any time:               cd &lt;your install folder&gt;
scripts\backup.bat   (Windows)
scripts/backup.sh    (Mac/Linux)              Backup files are plain SQL files. Copy them to an external drive or cloud storage for safe-keeping.                  12. Common problems and fixes                  &quot;Docker is not running&quot;              Open Docker Desktop, wait until the whale icon stops animating, then run the installer again.                  &quot;Port already in use&quot;              Another program (or another company you already installed) is using port 8080/4000/5432. Run the installer again and pick higher numbers (8081/4001/5433).                  &quot;git: command not found&quot;              Install Git for Windows from https://git-scm.com/download/win and run the installer again.                  &quot;Database not ready after 2 minutes&quot;              Your computer may be slow or low on memory. Wait a minute, then run the installer again â€” it is safe to re-run; it will pick up where it left off.                  &quot;I forgot an admin password&quot;              Run scripts/make-admin.bat (Windows) or scripts/make-admin.sh (Mac/Linux) inside the install folder. It will create or reset the admin user.                  &quot;The browser shows â€˜this site canâ€™t be reachedâ€™&quot;              Wait 30 seconds (the app needs a moment to start the first time after a reboot), then refresh. If it still doesnâ€™t work, run docker ps and confirm three containers are listed. If not, run docker compose --env-file .env.local up -d in the install folder.                  13. Uninstalling a company              To completely remove a company from this computer:               cd &lt;install folder&gt;
docker compose down -v              Then delete the install folder. The &quot;-v&quot; removes the database volume as well, so do this only if you are sure you have a backup.                  14. Getting help              If anything goes wrong, send your IT contact:                  A screenshot of the installer window, including any error message in red.                  The output of &quot;docker ps&quot; (run it in any command prompt).                  The contents of the file .env.local from your install folder (with the password lines removed).              That is normally enough to identify the issue within minutes.                  â€” End of guide â€”             
