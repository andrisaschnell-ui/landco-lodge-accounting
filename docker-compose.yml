version: '3.8'

services:
  app:
    build: .
    container_name: landco_lodge_app
    ports:
      - "8080:8080"
    stdin_open: true
    tty: true
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - VITE_PORT=8080
    restart: unless-stopped
