# LANACC environment example. Copy to .env (auto-managed) or set per-environment.
# The values below are safe to commit (publishable/anon only).

# --- Frontend (Vite) ---
VITE_SUPABASE_PROJECT_ID="yllodlsdldtttkftalwc"
VITE_SUPABASE_URL="https://yllodlsdldtttkftalwc.supabase.co"
VITE_SUPABASE_PUBLISHABLE_KEY="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InlsbG9kbHNkbGR0dHRrZnRhbHdjIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzU5NDA3NzIsImV4cCI6MjA5MTUxNjc3Mn0._LVfaD5cRTOVeJEgnWGmaBME9GvmdzSN8_5eh1W63qA"

# Where the React app reaches the local API. Leave empty to auto-detect from
# the browser hostname (recommended for LAN access from other PCs).
# VITE_API_URL=http://localhost:4000

# --- Backend / API container (cloud sync) ---
# REQUIRED for "Push Local → Cloud". Get it from Lovable Cloud → Backend → API keys.
# Do NOT commit your real value. Set this only in docker-compose.override.yml or shell env.
SUPABASE_SERVICE_ROLE_KEY=
